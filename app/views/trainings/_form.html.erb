<%= form_with(model: [@customer, @training], local: true) do |form| %>
  <div class="card-body">
    <div class="form-group">
      <%= form.label :number, "Número" %>
      <%= form.text_field :number, class: "form-control", :required => true %>
    </div>
    <div class="form-group">
      <%= form.label :start, "Fecha de Inicio" %>
      <%= form.date_field :start, class: "form-control", :required => true %>
    </div>
  </div>

  <div class="card-footer">
    <%= form.submit "Guardar", class: "btn btn-primary" %>
  </div>
<% end %>